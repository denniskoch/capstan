name: image

on:
  push:
    branches: [main]
    tags: ["v*"]
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

permissions:
  contents: read
  packages: write

jobs:
  image:
    name: build and push
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-qemu-action@v3

      - uses: docker/setup-buildx-action@v3

      - name: log in to ghcr
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: tags and labels
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            # Every push to main is edge: the current tip, not a release.
            type=edge,branch=main

            # A v1.2.3 tag publishes 1.2.3, 1.2 and 1, so a deployment can
            # pin as tightly or loosely as it wants.
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=semver,pattern={{major}},enable=${{ !startsWith(github.ref, 'refs/tags/v0.') }}

            # Immutable handle for a specific build, for pinning a rollback.
            type=sha,format=long
          labels: |
            org.opencontainers.image.title=capstan
            org.opencontainers.image.description=Authenticating TLS front door for a Docker host's socket
            org.opencontainers.image.licenses=AGPL-3.0-or-later

      - name: build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      # `latest` should mean "newest release", never "newest commit". The
      # metadata action's default latest rule would also fire on a prerelease
      # tag, so it is set explicitly here instead.
      - name: tag latest on a release
        if: startsWith(github.ref, 'refs/tags/v') && !contains(github.ref, '-')
        uses: docker/build-push-action@v6
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
